= semantic_form_for(@story) do |f|
  = error_messages_for :story
  %fieldset
    %ul
      = f.input :name, :required => true
      = f.input :description, :as => :text
      = f.commit_button f.object.new_record? ? 'Create Story' : 'Update Story', :button_html => { :id => nil }
